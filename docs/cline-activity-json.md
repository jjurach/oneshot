# Cline Activity JSON Schema

This document is intended to describe the schema of the "activity json" that is streamed from the cline executor.

Unfortunately, I was unable to find any information about the schema in the `tmp/cline` directory.
