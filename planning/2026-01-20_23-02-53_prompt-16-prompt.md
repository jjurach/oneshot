claude code json-schema

create a docs/ file with benefits and possible implementation details to
support this claude code feature:

  --json-schema: A newly introduced flag (v2.0+) that allows you to provide a
  schema file. Claude Code then uses constrained decoding to ensure the result is
  100% valid against your schema.

Research with google search and consult external/claude-code to provide more
background details to your theories.

